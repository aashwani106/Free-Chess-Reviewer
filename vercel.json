{
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/next",
        "config": {
          "installCommand": "npm install && npx @puppeteer/browsers install chrome@stable"
        }
      }
    ]
  } 